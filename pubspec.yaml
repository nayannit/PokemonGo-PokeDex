name: pokemon_go_pokedex
description: Pokemon GO PokeDex using LilyDex API
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  nested: ^1.0.0
  dio: ^5.9.0
  cached_network_image: ^3.4.1
  google_fonts: ^6.3.0
  flutter_animate: ^4.5.2
  gap: ^3.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

